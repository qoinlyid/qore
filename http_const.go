package qore

const (
	// HTTP header key for trace id.
	HTTP_HEADER_TRACE_ID = "X-Trace-ID"
	// HTTP context key for trace id
	HTTP_CONTEXT_TRACE_ID = "traceId"
	// HTTP context key for auth session.
	HTTP_CONTEXT_AUTH = "auth"
)
